package dev.notypie.impl.command

import io.kotest.core.spec.style.BehaviorSpec

class SlackRequestHandlerTest: BehaviorSpec({
    //CHECK Springboot test not required?
//    val restRequester: RestClientRequester = RestClientRequester(baseUrl = "https://slack.com/api/")
//    val slackRequestHandler: SlackRequestHandler = SlackRequestHandlerImpl(restRequester = restRequester, botToken = )

})