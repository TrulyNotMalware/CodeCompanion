<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-30 | Updated: 2026-08-30 -->

# domain/src/main/kotlin/dev/notypie

## Purpose
Package segment `dev.notypie`. In this module it contains only `domain/`; `:application` uses
`dev.notypie.application.*` and `:infrastructure` uses `dev.notypie.{impl,repository,templates,...}`
under the same prefix, so a symbol's module is determined by the next segment, not by this one.

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `domain/` | `dev.notypie.domain` — the four domain packages (see `domain/AGENTS.md`) |

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
