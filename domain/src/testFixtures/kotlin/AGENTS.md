<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-30 | Updated: 2026-08-30 -->

# domain/src/testFixtures/kotlin

## Purpose
Kotlin source root of the fixtures set. Packages mirror the main tree so a builder sits in the same package
as the type it constructs and the `internal` members of `domain/command` (contexts, `TestContext`) stay
reachable.

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `dev/` | Package root (`dev.notypie`) (see `dev/AGENTS.md`) |

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
