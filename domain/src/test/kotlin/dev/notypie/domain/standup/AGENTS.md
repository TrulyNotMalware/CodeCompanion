<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-28 | Updated: 2026-08-30 -->

# domain/standup (test)

## Purpose
Container for the standup aggregate specs. Mirrors main `domain/standup/`; only `entity/` has specs —
`dto/` and `enums/` carry no behaviour worth a spec of their own.

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `entity/` | `RoutineTest` and `StandupSessionTest` — construction limits and de-duplicating child collections (see `entity/AGENTS.md`) |

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
