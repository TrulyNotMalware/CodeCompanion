<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-28 | Updated: 2026-08-30 -->

# domain/meet (test)

## Purpose
Container for the meeting aggregate specs. Mirrors main `domain/meet/`; only `entity/` has specs —
the `dto/` package has no behaviour of its own and is covered through the fixtures that build it.

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `entity/` | `MeetingTest` — `Meeting` / `Member` construction, validation limits, `addParticipant` (see `entity/AGENTS.md`) |

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
